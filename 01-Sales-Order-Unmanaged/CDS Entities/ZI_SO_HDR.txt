@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Root View: Sales Order Header'
@Metadata.ignorePropagatedAnnotations: true
@Metadata.allowExtensions: true
define root view entity Zi_so_hdr 
as select from vbak
composition [0..*] of ZI_SO_item as _item
composition [0..*] of ZI_SO_PARTNER as _partner
{
  key vbeln,
      auart,     
      vkorg,
      vtweg,
      spart,
      kunnr,
      @Semantics.systemDate.createdAt: true      
      erdat,
      @Semantics.systemTime.createdAt: true      
      erzet,
      @Semantics.user.createdBy: true
      ernam,
      @Semantics.systemDate.createdAt: true      
      vdatu,
      @Semantics.systemTime.createdAt: true
      crossitem_prc_date,
      @Semantics.amount.currencyCode: 'waerk'
      netwr,  
      waerk,
      @Semantics.systemDate.createdAt: true           
      audat,
      @Semantics.systemDateTime.lastChangedAt: true
      aedat,   //Using VBAK-AEDAT (Change Date) as a proxy so behavior definition of the interface view can be achieved.
      
      _item,        // Make association public
      _partner      // Make association public
}
